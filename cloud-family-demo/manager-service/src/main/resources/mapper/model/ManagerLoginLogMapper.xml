<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kfpd.cloud.manager.dao.model.ManagerLoginLogDao">

    <insert id="insertLoginLog" parameterType="com.kfpd.cloud.manager.pojo.entity.ManagerLoginLog"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO manager_login_log
            (username, user_type, client_id, grant_type, client_ip, user_agent,
             device_type, browser, operating_system, login_at, login_result, failure_reason)
        VALUES
            (#{username}, #{userType}, #{clientId}, #{grantType},
             #{clientIp}, #{userAgent}, #{deviceType}, #{browser},
             #{operatingSystem}, #{loginAt}, #{loginResult}, #{failureReason})
    </insert>
</mapper>
