<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kfpd.cloud.manager.dao.SysPermissionDao">

    <resultMap id="SysPermissionResultMap" type="com.kfpd.cloud.manager.pojo.entity.SysPermission">
        <id property="id" column="id"/>
        <result property="permissionCode" column="permission_code"/>
        <result property="permissionName" column="permission_name"/>
        <result property="description" column="description"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="SysPermissionColumns">
        id, permission_code, permission_name, description, status, created_at, updated_at
    </sql>

    <select id="findAll" resultMap="SysPermissionResultMap">
        SELECT <include refid="SysPermissionColumns"/>
        FROM sys_permission
        ORDER BY id DESC
    </select>

    <select id="findById" resultMap="SysPermissionResultMap">
        SELECT <include refid="SysPermissionColumns"/>
        FROM sys_permission
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.kfpd.cloud.manager.pojo.entity.SysPermission"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_permission (permission_code, permission_name, description, status)
        VALUES (#{permissionCode}, #{permissionName}, #{description}, #{status})
    </insert>

    <update id="update" parameterType="com.kfpd.cloud.manager.pojo.entity.SysPermission">
        UPDATE sys_permission
        SET permission_code = #{permissionCode},
            permission_name = #{permissionName},
            description = #{description},
            status = #{status}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM sys_permission
        WHERE id = #{id}
    </delete>
</mapper>
