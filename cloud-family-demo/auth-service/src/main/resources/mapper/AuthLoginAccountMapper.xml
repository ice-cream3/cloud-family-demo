<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kfpd.cloud.auth.dao.AuthLoginAccountDao">

    <resultMap id="AuthLoginAccountResultMap" type="com.kfpd.cloud.auth.service.AuthLoginAccount">
        <constructor>
            <arg column="username" javaType="java.lang.String"/>
            <arg column="password_hash" javaType="java.lang.String"/>
            <arg column="user_type" javaType="java.lang.String"/>
            <arg javaType="java.util.List" select="findRolesByUsername" column="username"/>
            <arg javaType="java.util.List" select="findPermissionsByUsername" column="username"/>
        </constructor>
    </resultMap>

    <select id="findByUsername" resultMap="AuthLoginAccountResultMap">
        SELECT u.username,
               u.password_hash,
               CASE
                   WHEN EXISTS (
                       SELECT 1
                       FROM sys_user_role ur
                       INNER JOIN sys_role r ON r.id = ur.role_id
                       WHERE ur.user_id = u.id
                         AND r.status = 'ENABLED'
                         AND UPPER(r.role_code) = 'MANAGER'
                   ) THEN 'MANAGER'
                   ELSE 'API'
               END AS user_type
        FROM sys_user u
        WHERE u.username = #{username}
          AND u.status = 'ENABLED'
    </select>

    <select id="findRolesByUsername" resultType="java.lang.String">
        SELECT r.role_code
        FROM sys_role r
        INNER JOIN sys_user_role ur ON ur.role_id = r.id
        INNER JOIN sys_user u ON u.id = ur.user_id
        WHERE u.username = #{username}
          AND u.status = 'ENABLED'
          AND r.status = 'ENABLED'
        ORDER BY r.id DESC
    </select>

    <select id="findPermissionsByUsername" resultType="java.lang.String">
        SELECT DISTINCT p.permission_code
        FROM sys_permission p
        INNER JOIN sys_role_permission rp ON rp.permission_id = p.id
        INNER JOIN sys_role r ON r.id = rp.role_id
        INNER JOIN sys_user_role ur ON ur.role_id = r.id
        INNER JOIN sys_user u ON u.id = ur.user_id
        WHERE u.username = #{username}
          AND u.status = 'ENABLED'
          AND r.status = 'ENABLED'
          AND p.status = 'ENABLED'
        ORDER BY p.permission_code ASC
    </select>
</mapper>
